import 'package:flutter/material.dart';

class SettingsBodyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('SettingsBodyWidget'),
    );
  }
}