import 'package:flutter/material.dart';

class HistoryBodyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('HistoryBodyWidget'),
    );
  }
}